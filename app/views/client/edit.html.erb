<%= render :partial => "shared/message" %>
<%= form_tag  url_for(:controller => :client, :action => :save), method: 'post', multipart: true, class: 'form-horizontal', role: 'form' do %>
    <fieldset>
        <legend>Editar Perfil</legend>
        <div class="form-group">
            <label for="picture" class="col-sm-2 control-label">Foto</label>
            <div class="col-sm-10">
                <%= image_tag "/uploads/#{@account.picture}", id: 'preview-picture', 'data-file': 'picture' %>
                <%= file_field_tag 'picture', class: 'picture-field'%>
            </div>
        </div>
		<div class="form-group">
            <label for="first_name" class="col-sm-2 control-label">Nombre</label>
            <div class="col-sm-10">
                <%= text_field_tag 'first_name', @client.first_name, class: 'form-control', placeholder: 'Nombre' %>
            </div>
        </div>
        <div class="form-group">
            <label for="first_name" class="col-sm-2 control-label">Apellido</label>
            <div class="col-sm-10">
                <%= text_field_tag 'last_name', @client.last_name, class: 'form-control', placeholder: 'Apellido' %>
            </div>
        </div>
        <div class="form-group">
            <label for="first_name" class="col-sm-2 control-label">Correo</label>
            <div class="col-sm-10">
                <%= email_field_tag 'email', @email.email, class: 'form-control', placeholder: 'Correo Electrónico' %>
            </div>
        </div>
        <div class="form-group">
            <label for="first_name" class="col-sm-2 control-label">Tel&eacute;fono</label>
            <div class="col-sm-10">
                <%= text_field_tag 'phone_number', @client.phone_number, class: 'form-control', placeholder: 'Teléfono' %>
            </div>
        </div>
        <div id="hidden_fields">
            <%= hidden_field_tag 'client_id', @client.id %>
        </div>
        <div class="btn-group pull-right">
            <%= submit_tag 'Guardar', class: 'btn btn-primary'%>
        </div>
    </fieldset>
<% end %>