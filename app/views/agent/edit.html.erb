<section class="center col-lg-8 col-md-8 col-sm-12 col-xs-12">
	<%= form_tag  :url => {:action => "save"}, :html => {:class=>'form-horizontal', :role=>'form'} do %>
		<div class="form-group">
            <label for="first_name" class="col-sm-2 control-label">Nombre</label>
            <div class="col-sm-10">
                <%= text_field_tag :first_name, :class=>'form-control', :placeholder => 'Nombre', :value => @agent.first_name %>
            </div>
        </div>
        <div class="form-group">
            <label for="first_name" class="col-sm-2 control-label">Apellido</label>
            <div class="col-sm-10">
                <%= text_field_tag :last_name, :class=>'form-control', :placeholder => 'Apellido', :value => @agent.last_name %>
            </div>
        </div>
        <div class="form-group">
            <label for="first_name" class="col-sm-2 control-label">Correo</label>
            <div class="col-sm-10">
                <%= email_field_tag :first_name, :class=>'form-control', :placeholder => 'Correo Electrónico', :value => @email.email %>
            </div>
        </div>
	<% end %>
</section>